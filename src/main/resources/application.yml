spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loan-calculator?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: example
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: mysql

server:
  servlet:
    context-path: /leanpay